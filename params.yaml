split:
  test_size: 0.2
  random_state: 42

grid_search:
  cv: 5
  n_jobs: -1
  scoring: neg_mean_squared_error
  param_grid:
    n_estimators: [100, 200]
    max_depth: [2, 3, 4]
    learning_rate: [0.01, 0.1]
    min_samples_leaf: [1, 3]

train:
  random_state: 42
